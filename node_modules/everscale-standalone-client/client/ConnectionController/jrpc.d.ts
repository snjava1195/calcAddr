import type * as nt from 'nekoton-wasm';
/**
 * @category Client
 */
export declare type JrpcSocketParams = {
    endpoint: string;
};
export declare class JrpcSocket {
    connect(clock: nt.ClockWithOffset, params: JrpcSocketParams): Promise<nt.JrpcConnection>;
}
